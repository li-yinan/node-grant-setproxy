二进制文件来源于
https://github.com/getlantern/proxysetup
原理简介:
我们的目标是想无密码修改代理，但是mac下networksetup这个命令不是root身份就必须要密码
linux和unix都有一个厉害的东西，可以让一个root身份的程序被其他身份执行时依然是root身份
这个东西叫做setuid
这个东西只有写代码来实现，没法用shell直接实现，所以我找了一个c语言写的代码
这个代码里最核心的一句话就是setUid(0)
这句代码的意思是切换身份到root，没有这句话，这个程序执行身份就是当前用户身份

所以我们就需要在程序第一次运行的时候把这个二进制代码变为owner是root，并且属于wheel组
再给这个程序chmod u+s ./proxysetup，开启uid位

之后就可以无密码操作了

由于可以无密码操作root权限的东西，所以安全性还是需要保证的，千万别用这个方案给太大权限的程序
像我们这个二进制程序就只能设置一下代理，其他命令都不行
